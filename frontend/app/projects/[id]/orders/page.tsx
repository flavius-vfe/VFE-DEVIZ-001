"use client";import{useCallback,useEffect,useState}from"react";import{useParams}from"next/navigation";import{api}from"../../../../lib/api";const money=(x:any)=>Number(x||0).toLocaleString("ro-RO",{minimumFractionDigits:2});export default function Page(){const{id}=useParams<{id:string}>(),[orders,setOrders]=useState<any[]>([]),[project,setProject]=useState<any>(),[selected,setSelected]=useState<any>(),[error,setError]=useState("");const load=useCallback(async()=>{try{setProject(await api(`/api/projects/${id}`));setOrders(await api(`/api/projects/${id}/orders`))}catch(e){setError(e instanceof Error?e.message:"Eroare.")}},[id]);useEffect(()=>{load()},[load]);async function status(o:any,s:string){await api(`/api/projects/${id}/orders/${o.id}/status`,{method:"POST",body:JSON.stringify({status:s})});await load()}return <main><div className="breadcrumbs"><a href="/">Proiecte</a><span>/</span><a href={`/projects/${id}`}>{project?.name}</a><span>/</span><strong>Comenzi materiale</strong></div><h1>Comenzi materiale</h1>{error&&<div className="error card">{error}</div>}{orders.length===0?<div className="empty">Nu există comenzi. Selectează o ofertă sau creează comenzi dintr-un plan de aprovizionare.</div>:<div className="card table-wrap"><table><thead><tr><th>Nr. comandă</th><th>Furnizor</th><th>Data</th><th>Status</th><th>Produse</th><th>Transport</th><th>Total cu TVA</th><th>Livrare solicitată</th><th></th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.order_number}</td><td>{o.supplier_name}</td><td>{new Date(o.created_at).toLocaleDateString("ro-RO")}</td><td>{o.status}</td><td>{o.items.length}</td><td>{money(o.transport_cost)}</td><td>{money(o.total_gross)}</td><td>{o.requested_delivery_date?new Date(o.requested_delivery_date).toLocaleDateString("ro-RO"):"—"}</td><td className="toolbar"><button onClick={()=>setSelected(o)}>Deschide</button>{o.status==="DRAFT"&&<button onClick={()=>status(o,"READY_TO_ORDER")}>Marchează gata de comandă</button>}{o.status==="READY_TO_ORDER"&&<button onClick={()=>status(o,"ORDERED")}>Marchează comandată</button>}{o.status!=="ORDERED"&&o.status!=="CANCELLED"&&<button className="danger" onClick={()=>status(o,"CANCELLED")}>Anulează</button>}</td></tr>)}</tbody></table></div>}{selected&&<div className="card"><div className="toolbar push"><h2>{selected.order_number}</h2><div><a href={`/api/projects/${id}/orders/${selected.id}/export.pdf`}>PDF</a> <a href={`/api/projects/${id}/orders/${selected.id}/export.xlsx`}>XLSX</a></div></div><p><strong>Furnizor:</strong> {selected.supplier_name}</p><p><strong>Adresă livrare:</strong> {selected.delivery_address}</p><table><thead><tr><th>Produs</th><th>SKU</th><th>Cantitate</th><th>UM</th><th>Ambalaj</th><th>Preț</th><th>Discount</th><th>Total</th></tr></thead><tbody>{selected.items.map((x:any)=><tr key={x.id}><td>{x.description}</td><td>{x.sku||"—"}</td><td>{x.quantity}</td><td>{x.unit}</td><td>{x.package_count?`${x.package_count} × ${x.package_quantity}`:"—"}</td><td>{x.unit_price_net}</td><td>{x.discount_net}</td><td>{x.line_total_gross}</td></tr>)}</tbody></table><div className="results-grid"><div>Subtotal fără TVA<strong>{money(selected.subtotal_net)}</strong></div><div>Discount<strong>{money(selected.discount_total)}</strong></div><div>Transport<strong>{money(selected.transport_cost)}</strong></div><div>TVA<strong>{money(selected.vat_total)}</strong></div><div>TOTAL CU TVA<strong>{money(selected.total_gross)}</strong></div></div>{selected.status==="ORDERED"&&<button onClick={()=>api(`/api/projects/${id}/orders/${selected.id}/revision`,{method:"POST"}).then(load)}>Creează revizie</button>}</div>}</main>}
